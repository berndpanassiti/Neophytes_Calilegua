---
title: "workingdata"
output: pdf_document
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
source("r-code/00_functions.R") # data_summary
load("data/workingdata.RData")
load("data/individualSDM.RData")
source("r-code/00_functions.R")
using("ggplot2","ellipse","magrittr","tidyverse","sjPlot")
# ellipse: #library(GGally) problem with NA's
# magrittr: # for %<>%

mytheme <-  theme(axis.title = element_text(size =18),
        axis.text = element_text(size =14),
        strip.text= element_text(size = 18),
        legend.position = "none",
        plot.title = element_text(hjust = 0.5))
```

```{r}
pp1 <- plot_model(Cyn.dac.glmer,transform = NULL)
pp2 <- plot_model(Meg.max.glmer,transform = NULL)+theme(axis.text.y = element_blank())
pp3 <- plot_model(Ric.com.glmer,transform = NULL)+theme(axis.text.y = element_blank())
grid.arrange(pp1,pp2,pp3,nrow=1)
```

